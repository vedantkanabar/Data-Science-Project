# Resampling Experimentation

- This folder contains code for the resampling experiment
- The `resampling.py` file contains code to read the features from `../combined_dataset/combined_encoded.csv` and run the resampling methods on it to train a Random Forest Classifier
- The numeric metrics are in `results.txt` and the plots are in files `Confusion Matrix - <Resampling Method>.png`